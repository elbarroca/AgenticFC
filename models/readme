from pathlib import Path

# Define a detailed purpose for each file as described in the image
file_descriptions = {
    "backtest_engine.py": "Handles time-series cross-validation for model evaluation using rolling/expanding windows. Includes betting simulation and summary evaluation across all models.",
    "config.py": "Stores global configuration variables such as hyperparameters, file paths, odds columns, and constants shared across the framework.",
    "elo.py": "Calculates and maintains Elo ratings for teams over time to reflect dynamic team strength. Used as a feature in model training.",
    "features.py": "Core feature engineering logic. Generates rolling statistics, form metrics, and integrates external features like Elo ratings or odds.",
    "metrics.py": "Implements model evaluation metrics such as accuracy, log loss, and ROI. Used during backtesting to compare model performance.",
    "base_model.py": "Abstract base class defining a standard interface for all models (fit, predict, predict_proba) to ensure consistency across implementations.",
    "bayesian_model.py": "Bayesian belief update model. Adjusts pre-match predictions based on new evidence using Bayes' theorem.",
    "ensemble_model.py": "Combines predictions from multiple models (e.g., RF, GBM, LSTM) using strategies like weighted voting or averaging for robust outputs.",
    "gradient_boosting_model.py": "Wrapper around XGBoost or LightGBM. Trains and evaluates high-performance gradient boosting models on tabular features.",
    "lstm_model.py": "Deep learning model based on LSTM architecture. Captures temporal patterns from sequential match history (e.g., last 15 games).",
    "markov_model.py": "Implements Markov chains to model transitions in match states (e.g., win/draw/loss sequences) and predict next likely outcomes.",
    "model_registry.py": "Central model loader. Maps model name strings to their corresponding classes, allowing flexible model instantiation during training or backtest.",
    "monte_carlo_model.py": "Performs Monte Carlo simulations using Poisson-distributed goal models to estimate probabilistic outcomes like W/D/L, over/under, and scorelines.",
    "poisson_model.py": "Uses Poisson distribution to model and predict final scorelines based on expected goals per team (lambda values).",
    "random_forest_model.py": "Random forest model using sklearn. Learns from structured features like odds, form, and Elo ratings for classification or regression.",
    "train_model.py": "Main entry script to train or test one or more models. Initializes data, runs training, and logs output performance."
}
